[package]
name = "pico2w-hello"
version = "0.1.0"
edition = "2021"

[dependencies]
cortex-m = "0.7"
cortex-m-rt = "0.7"

panic-halt = "1"

embedded-hal = "1"

rp235x-hal = { version = "0.3", features = ["binary-info"] }

[profile.release]
debug = 2